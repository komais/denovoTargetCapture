getwd()
dir()
d <- read.table('diatomsF.txt', header=T, sep='\t');
dim(d)
head(d)
names(d)
records25<-d[d$sample_age>24 & d$sample_age<=25, ];
dim(records25)
length(unique(records25$list));
records25<-d[d$sample_age>24 & d$sample_age<=25, ];#
samples25<-length(unique(records25$list));#
samples25;
species25<-length(unique(records25$species));
species25;
seq(30, 0, by = -1);
breakvec <- seq(30, 0, by = -1);
numBins <- length(breakvec) - 1;
length(breakvec)
numBins
spcounts <- numeric(numBins);
spcounts
listcounts <- numeric(numBins);
length(listcounts)
breakvec
i <- 10
cat(i)
cat(i, 'i')
cat('i = ', i)
cat('i =', i, sep='\t')
cat('i =', i, sep='\t\t\t\t\t')
cat('i = ', i, '\n')
cat('i = ', i, '\n', 'the cat in the hat')
cat('i = ', i, '\n\n\n', 'the cat in the hat')
i <- 1;
i1
i
minbin <- breakvec[i];#
maxbin <- breakvec[i + 1];
minbin
maxbin
cat('i = ', i, '\tMin: ', minbin, '\tMax: ', maxbin, '\n')
i <- 1;#
minbin <- breakvec[i];#
maxbin <- breakvec[i + 1];#
cat('i = ', i, '\tMin: ', minbin, '\tMax: ', maxbin, '\n')
cat(i = , i, '\tMin: ', minbin, '\tMax: ', maxbin, '\n')
minbin <- breakvec[i];#
maxbin <- breakvec[i + 1];#
cat(i = , i, '\tMin: ', minbin, '\tMax: ', maxbin, '\n')
minbin <- breakvec[i];
maxbin <- breakvec[i + 1];
cat(i = , i, '\tMin: ', minbin, '\tMax: ', maxbin, '\n')
for (i in 1:numBins){#
i <- 1;#
minbin <- breakvec[i];#
maxbin <- breakvec[i + 1];#
cat('i = ', i, '\tMin: ', minbin, '\tMax: ', maxbin, '\n')#
#
}
for (i in 1:numBins){#
minbin <- breakvec[i];#
maxbin <- breakvec[i + 1];#
cat('i = ', i, '\tMin: ', minbin, '\tMax: ', maxbin, '\n')#
#
}
names(d)
i <- 1;
minbin <- breakvec[i];
maxbin <- breakvec[i + 1];
cat('i = ', i, '\tMin: ', minbin, '\tMax: ', maxbin, '\n')
tempdf <- d[d$sample_age < minbin & d$sample_age >= maxbin, ];
spcounts[i] <- length(unique(tempdf$species));
listcounts[i] <- length(unique(tempdf$list));
spcounts[1]
spcounts
listcounts
for (i in 1:numBins){#
#
minbin <- breakvec[i];#
maxbin <- breakvec[i + 1];#
cat('i = ', i, '\tMin: ', minbin, '\tMax: ', maxbin, '\n')#
tempdf <- d[d$sample_age < minbin & d$sample_age >= maxbin, ];#
spcounts[i] <- length(unique(tempdf$species));#
listcounts[i] <- length(unique(tempdf$list));#
}
spcounts
listcounts
quartz.options(height=8, width=8, dpi=72);#
plot.new();#
par(mar=c(6, 6, 1, 6));
quartz.options(height=8, width=8, dpi=72);#
plot.new();#
par(mar=c(6, 6, 1, 6));#
#
# set up plot window#
plot.window(xlim=c(max(breakvec), min(breakvec)), ylim=c(0, 1.05*max(spcounts)));
quartz.options(height=8, width=8, dpi=72);#
plot.new();#
par(mar=c(6, 6, 1, 6));#
#
# set up plot window#
plot.window(xlim=c(max(breakvec), min(breakvec)), ylim=c(0, 1.05*max(spcounts)));#
#
#add axes#
axis(side=1, at= seq(35, -5, by = -5), cex.axis =1.1 );
max(spcounts)
quartz.options(height=8, width=8, dpi=72);#
plot.new();#
par(mar=c(6, 6, 1, 6));#
#
# set up plot window#
plot.window(xlim=c(max(breakvec), min(breakvec)), ylim=c(0, 1.05*max(spcounts)));#
#
#add axes#
axis(side=1, at= seq(35, -5, by = -5), cex.axis =1.1 );#
axis(side=2, at=seq(-100, 400, by=100), cex.axis=1.1)
quartz.options(height=8, width=8, dpi=72);#
plot.new();#
par(mar=c(6, 6, 1, 6));#
#
# set up plot window#
plot.window(xlim=c(max(breakvec), min(breakvec)), ylim=c(0, 1.05*max(spcounts)));#
#
#add axes#
axis(side=1, at= seq(35, -5, by = -5), cex.axis =1.1 );#
axis(side=2, at=seq(-100, 400, by=100), cex.axis=1.1, las=1)
binvec <- seq(29.5, 0.5, length.out = numBins);
binvec
quartz.options(height=8, width=8, dpi=72);#
plot.new();#
par(mar=c(6, 6, 1, 6));#
#
# set up plot window#
plot.window(xlim=c(max(breakvec), min(breakvec)), ylim=c(0, 1.05*max(spcounts)));#
#
binvec <- seq(29.5, 0.5, length.out = numBins);#
points(binvec, spcounts, pch=21, bg='red', cex=1.5);#
#
#add axes#
axis(side=1, at= seq(35, -5, by = -5), cex.axis =1.1 );#
axis(side=2, at=seq(-100, 400, by=100), cex.axis=1.1, las=1);
mtext(side=1, text='MY before present', line=3.5, cex=1.5);
mtext(side=2, text='Species richness', line=3.5, cex=1.5);
mtext(side=1, text='MY before present', line=3.5, cex=1.5, font=3);
quartz.options(height=8, width=8, dpi=72);#
plot.new();#
par(mar=c(6, 6, 1, 6));#
#
# set up plot window#
plot.window(xlim=c(max(breakvec), min(breakvec)), ylim=c(0, 1.05*max(spcounts)));#
#
binvec <- seq(29.5, 0.5, length.out = numBins);#
points(binvec, spcounts, pch=21, bg='red', cex=1.5);#
#
#add axes#
axis(side=1, at= seq(35, -5, by = -5), cex.axis =1.1 );#
axis(side=2, at=seq(-100, 400, by=100), cex.axis=1.1, las=1);#
mtext(side=1, text='MY before present', line=3.5, cex=1.5, font=3);#
mtext(side=2, text='Species richness', line=3.5, cex=1.5,font=3);
quartz.options(height=8, width=8, dpi=72);#
plot.new();#
par(mar=c(6, 6, 1, 6));#
#
# set up plot window#
plot.window(xlim=c(max(breakvec), min(breakvec)), ylim=c(0, 1.05*max(spcounts)));#
#
binvec <- seq(29.5, 0.5, length.out = numBins);#
points(binvec, spcounts, pch=21, bg='red', cex=1.5);#
#
#add axes#
axis(side=1, at= seq(35, -5, by = -5), cex.axis =1.1 );#
axis(side=2, at=seq(-100, 400, by=100), cex.axis=1.1, las=1);#
mtext(side=1, text='MY before present', line=3.5, cex=1.5, font=4);#
mtext(side=2, text='Species richness', line=3.5, cex=1.5,font=4);
par(new=TRUE);
listvec
max(listcounts)
par(new=TRUE);#
par(mar=c(6, 6, 1, 6));#
plot.window(xlim=c(max(breakvec), min(breakvec)), ylim=c(0, max(listcounts)*1.05));#
lines(x=binvec, y=listcounts, lwd=2, col='gray50')
max(listcounts)
axis(4, at=seq(-100, 500, by=100), cex.axis=1.1)
axis(4, at=seq(-100, 500, by=100), cex.axis=1.1, las=1)
quartz.options(height=8, width=8, dpi=72);#
plot.new();#
par(mar=c(6, 6, 1, 6));#
#
# set up plot window#
plot.window(xlim=c(max(breakvec), min(breakvec)), ylim=c(0, 1.05*max(spcounts)));#
#
binvec <- seq(29.5, 0.5, length.out = numBins);#
points(binvec, spcounts, pch=21, bg='red', cex=1.5);#
#
#add axes#
axis(side=1, at= seq(35, -5, by = -5), cex.axis =1.1 );#
axis(side=2, at=seq(-100, 400, by=100), cex.axis=1.1, las=1);#
mtext(side=1, text='MY before present', line=3.5, cex=1.5, font=4);#
mtext(side=2, text='Species richness', line=3.5, cex=1.5,font=4);#
#
par(new=TRUE);#
par(mar=c(6, 6, 1, 6));#
plot.window(xlim=c(max(breakvec), min(breakvec)), ylim=c(0, max(listcounts)*1.05));#
lines(x=binvec, y=listcounts, lwd=2, col='gray50')#
axis(4, at=seq(-100, 500, by=100), cex.axis=1.1, las=1)
mtext(side=4, text='Number of lists', line=3.5, cex=1.5)
mtext(side=4, text='Number of lists', line=3.5, cex=1.5, font=4)
#
quartz.options(height=8, width=8, dpi=72);#
plot.new();#
par(mar=c(6, 6, 1, 6));#
#
# set up plot window#
plot.window(xlim=c(max(breakvec), min(breakvec)), ylim=c(0, 1.05*max(spcounts)));#
#
binvec <- seq(29.5, 0.5, length.out = numBins);#
points(binvec, spcounts, pch=21, bg='red', cex=1.5);#
#
#add axes#
axis(side=1, at= seq(35, -5, by = -5), cex.axis =1.1 );#
axis(side=2, at=seq(-100, 400, by=100), cex.axis=1.1, las=1);#
mtext(side=1, text='MY before present', line=3.5, cex=1.5, font=4);#
mtext(side=2, text='Species richness', line=3.5, cex=1.5,font=4);#
#
par(new=TRUE);#
par(mar=c(6, 6, 1, 6));#
plot.window(xlim=c(max(breakvec), min(breakvec)), ylim=c(0, max(listcounts)*1.05));#
lines(x=binvec, y=listcounts, lwd=2, col='gray50')#
axis(4, at=seq(-100, 500, by=100), cex.axis=1.1, las=1)#
mtext(side=4, text='Number of lists', line=3.5, cex=1.5, font=4)
breakvec <- seq(30, 0, by = -0.25);
breakvec
numBins <- length(breakvec) - 1;
numBins
spcounts <- numeric(numBins);
listcounts <- numeric(numBins);
for (i in 1:numBins){#
#
minbin <- breakvec[i];#
maxbin <- breakvec[i + 1];#
#cat('i = ', i, '\tMin: ', minbin, '\tMax: ', maxbin, '\n')#
tempdf <- d[d$sample_age < minbin & d$sample_age >= maxbin, ];#
spcounts[i] <- length(unique(tempdf$species));#
listcounts[i] <- length(unique(tempdf$list));#
}
spcounts
quartz.options(height=8, width=8, dpi=72);#
plot.new();#
par(mar=c(6, 6, 1, 6));#
#
# set up plot window#
plot.window(xlim=c(max(breakvec), min(breakvec)), ylim=c(0, 1.05*max(spcounts)));#
#
binvec <- seq(29.875, 0.125, length.out = numBins);#
points(binvec, spcounts, pch=21, bg='red', cex=1.5);#
#
#add axes#
axis(side=1, at= seq(35, -5, by = -5), cex.axis =1.1 );#
axis(side=2, at=seq(-100, 400, by=100), cex.axis=1.1, las=1);#
mtext(side=1, text='MY before present', line=3.5, cex=1.5, font=4);#
mtext(side=2, text='Species richness', line=3.5, cex=1.5,font=4);
greeterFunction <- function(theName){#
cat('Hello, ', theName);#
}
ls()
greeterFunction('Charlie');
greeterFunction('snoopy');
greeterFunction('Sean');
greeterFunction(352);
greeterFunction(pi);
greeterFunction(runif(10));
greeterFunction();
greeterFunction <- function(theName = 'Elvira'){#
cat('Hello, ', theName);#
}
greeterFunction('charlie')
greeterFunction()
mtext.default
mtext
?rnorm
greeterFunction(theName = 'Elvira'){#
cat('Hello, ', theName);#
#
return(theName);#
}
greeterFunction <- function(theName = 'Elvira'){#
cat('Hello, ', theName);#
#
return(theName);#
}
greeterFunction()
x <- greeterFunction()
x
x <- greeterFunction('charlie')
x
multiplier <- function(n1, n2){#
#
return(n1 * n2);#
#
}
multiplier(n1= 10, n2 = 250)
n1
n2
n3 <- 5
multiplier <- function(n1, n2){#
#
prod <- n1 * n2 * n3;#
return(prod);#
#
}
multiplier(5, 2)
rm(n3)
multiplier(5,2)
ls()
greeterFunction
q()
